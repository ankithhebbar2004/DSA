class Solution(object):
    def numJewelsInStones(self, jewels, stones):
        count = 0
        for vec in stones:
            if vec in jewels:
                count+=1
        return count
        
